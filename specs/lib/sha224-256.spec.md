# lib/sha224-256.c Specification

## Source Context

- Source: `lib/sha224-256.c`
- Related Headers: `lib/sha.h`, `lib/sha-private.h`, `lib/compat.h`
- Related Tests: `none`
- Related Dependencies: GitNexus context shows SHA-224 wrappers call SHA-256 shared implementations; `USHAReset`, `USHAInput`, `USHAFinalBits`, and `USHAResult` call SHA-224/SHA-256 APIs; downstream helpers include `SHA224_256Reset`, `SHA224_256ProcessMessageBlock`, `SHA224_256Finalize`, and `SHA224_256ResultN`.
- Build/Compile Context: C source included in the core library; `USE_SHA224` controls SHA-224 public wrappers and initial constants, while SHA-256 APIs are always present through `lib/sha.h`.

## Interface Summary

| Interface | Kind | Signature | Decision | Reason |
| --- | --- | --- | --- | --- |
| SHA224Reset | function | `int SHA224Reset (SHA224Context * context)` | Include | `USE_SHA224` 启用时的 SHA-224 上下文初始化入口，影响统一 SHA、HMAC 和 SMB2 认证相关哈希流程。 |
| SHA224Input | function | `int SHA224Input (SHA224Context * context, const uint8_t * message_array, size_t length)` | Include | `USE_SHA224` 启用时的 SHA-224 增量输入入口，复用 SHA-256 输入逻辑并对调用方暴露错误码和状态迁移。 |
| SHA224FinalBits | function | `int SHA224FinalBits (SHA224Context * context, const uint8_t message_bits, size_t length)` | Include | `USE_SHA224` 启用时的非字节对齐尾部输入入口，复用 SHA-256 final bits 逻辑并暴露长度边界。 |
| SHA224Result | function | `int SHA224Result (SHA224Context * context, uint8_t Message_Digest[SHA224HashSize])` | Include | `USE_SHA224` 启用时输出 28 字节摘要并触发最终化，调用方可观察输出大小和错误码。 |
| SHA256Reset | function | `int SHA256Reset (SHA256Context * context)` | Include | SHA-256 上下文初始化入口，GitNexus impact 显示 CRITICAL，上游覆盖 USHA、HMAC 和 SMB2 session/negotiation 流程。 |
| SHA256Input | function | `int SHA256Input (SHA256Context * context, const uint8_t * message_array, size_t length)` | Include | SHA-256 增量输入入口，处理零长度、空指针、已计算状态、已损坏状态和消息块压缩。 |
| SHA256FinalBits | function | `int SHA256FinalBits (SHA256Context * context, const uint8_t message_bits, size_t length)` | Include | SHA-256 非字节对齐尾部输入入口，处理 1 到 7 位输入、状态错误和最终化。 |
| SHA256Result | function | `int SHA256Result (SHA256Context * context, uint8_t Message_Digest[SHA256HashSize])` | Include | SHA-256 摘要输出入口，必要时最终化上下文并写入 32 字节 big-endian 摘要。 |
| SHA224_256Finalize | function | `static void SHA224_256Finalize (SHA256Context * context, uint8_t Pad_Byte)` | Skip | 静态内部 helper，仅由本文件公开 Result/FinalBits 路径调用，无独立外部接口契约。 |
| SHA224_256PadMessage | function | `static void SHA224_256PadMessage (SHA256Context * context, uint8_t Pad_Byte)` | Skip | 静态内部 padding helper，行为归属到 Result/FinalBits 公开接口。 |
| SHA224_256ProcessMessageBlock | function | `static void SHA224_256ProcessMessageBlock (SHA256Context * context)` | Skip | 静态内部压缩 helper，行为归属到 Input/Result/FinalBits 公开接口。 |
| SHA224_256Reset | function | `static int SHA224_256Reset (SHA256Context * context, uint32_t * H0)` | Skip | 静态内部 reset helper，行为归属到 SHA224Reset 和 SHA256Reset。 |
| SHA224_256ResultN | function | `static int SHA224_256ResultN (SHA256Context * context, uint8_t Message_Digest[], int HashSize)` | Skip | 静态内部摘要输出 helper，行为归属到 SHA224Result 和 SHA256Result。 |

## Data Model Summary

| Type/Macro | Kind | Definition | Notes |
| --- | --- | --- | --- |
| SHA256_H0 | static data | `lib/sha224-256.c:77` | SHA-256 FIPS-180-2 初始哈希值数组，供 SHA256Reset 使用。 |
| SHA224_H0 | static data | `lib/sha224-256.c:84` | `USE_SHA224` 启用时的 SHA-224 初始哈希值数组，供 SHA224Reset 使用。 |
| SHA256_SHR | macro | `lib/sha224-256.c:44` | 内部 32 位右移宏，用于消息调度。 |
| SHA256_ROTL | macro | `lib/sha224-256.c:45` | 内部 32 位循环左移宏。 |
| SHA256_ROTR | macro | `lib/sha224-256.c:47` | 内部 32 位循环右移宏，用于 SHA-256 sigma/SIGMA 计算。 |
| SHA256_SIGMA0 | macro | `lib/sha224-256.c:51` | 内部 SHA-256 大写 SIGMA0 变换宏。 |
| SHA256_SIGMA1 | macro | `lib/sha224-256.c:53` | 内部 SHA-256 大写 SIGMA1 变换宏。 |
| SHA256_sigma0 | macro | `lib/sha224-256.c:55` | 内部 SHA-256 小写 sigma0 变换宏。 |
| SHA256_sigma1 | macro | `lib/sha224-256.c:57` | 内部 SHA-256 小写 sigma1 变换宏。 |
| SHA224_256AddLength | macro | `lib/sha224-256.c:63` | 内部长度累加宏，低 32 位溢出时递增高 32 位，高位再次溢出时标记 `Corrupted`。 |

## ADDED Requirements

### Requirement: SHA224Reset initialize SHA-224 context
系统 MUST 在 `USE_SHA224` 启用时使用 SHA-224 初始哈希常量重置上下文，并返回与共享 reset helper 相同的 SHA 错误码。

#### Scenario: 成功初始化 SHA-224 上下文
- **GIVEN** 调用方提供非空 `SHA224Context` 指针且编译启用 `USE_SHA224`
- **WHEN** 调用 `SHA224Reset(context)`
- **THEN** 函数返回 `shaSuccess`，清零长度和块索引，设置 SHA-224 初始哈希值，并将 `Computed` 与 `Corrupted` 置为 0

Trace: `lib/sha224-256.c:SHA224Reset`, `lib/sha224-256.c:SHA224_256Reset`

#### Scenario: 拒绝空 SHA-224 上下文
- **GIVEN** 编译启用 `USE_SHA224` 且调用方传入空 `context`
- **WHEN** 调用 `SHA224Reset(context)`
- **THEN** 函数返回 `shaNull` 且不写入上下文状态

Trace: `lib/sha224-256.c:SHA224Reset`, `lib/sha224-256.c:SHA224_256Reset`

### Requirement: SHA224Input append SHA-224 octets
系统 MUST 在 `USE_SHA224` 启用时把 SHA-224 输入转交给共享 SHA-256 输入逻辑，并保持相同的零长度、空指针、状态错误和长度累加语义。

#### Scenario: SHA-224 输入复用共享输入逻辑
- **GIVEN** 已通过 `SHA224Reset` 初始化的上下文和非空消息缓冲区
- **WHEN** 调用 `SHA224Input(context, message_array, length)` 且 `length` 大于 0
- **THEN** 函数按字节追加消息、按 8 bit 累加长度、在 64 字节块满时处理消息块，并返回共享输入逻辑产生的错误码

Trace: `lib/sha224-256.c:SHA224Input`, `lib/sha224-256.c:SHA256Input`

### Requirement: SHA224FinalBits append SHA-224 trailing bits
系统 MUST 在 `USE_SHA224` 启用时把 SHA-224 尾部 bit 输入转交给共享 SHA-256 final bits 逻辑，并保持 1 到 7 bit 的长度约束和最终化语义。

#### Scenario: SHA-224 final bits 复用共享最终化逻辑
- **GIVEN** 已通过 `SHA224Reset` 初始化且尚未计算摘要的上下文
- **WHEN** 调用 `SHA224FinalBits(context, message_bits, length)` 且 `length` 位于 1 到 7
- **THEN** 函数累加尾部 bit 长度，按高位有效 bit 加入终止标记并最终化上下文，返回共享 final bits 逻辑产生的错误码

Trace: `lib/sha224-256.c:SHA224FinalBits`, `lib/sha224-256.c:SHA256FinalBits`

### Requirement: SHA224Result return 224-bit digest
系统 MUST 在 `USE_SHA224` 启用时输出 28 字节 SHA-224 摘要，并在上下文尚未计算时先完成 padding、压缩和敏感缓冲区清理。

#### Scenario: 输出 SHA-224 摘要
- **GIVEN** 已通过 SHA-224 输入路径填充的上下文和非空 `Message_Digest` 缓冲区
- **WHEN** 调用 `SHA224Result(context, Message_Digest)`
- **THEN** 函数在需要时最终化上下文，按 big-endian 字节顺序写入 `SHA224HashSize` 个摘要字节，并返回 `shaSuccess`

Trace: `lib/sha224-256.c:SHA224Result`, `lib/sha224-256.c:SHA224_256ResultN`

### Requirement: SHA256Reset initialize SHA-256 context
系统 MUST 使用 SHA-256 初始哈希常量重置上下文，并返回与共享 reset helper 相同的 SHA 错误码。

#### Scenario: 成功初始化 SHA-256 上下文
- **GIVEN** 调用方提供非空 `SHA256Context` 指针
- **WHEN** 调用 `SHA256Reset(context)`
- **THEN** 函数返回 `shaSuccess`，清零长度和块索引，设置 SHA-256 初始哈希值，并将 `Computed` 与 `Corrupted` 置为 0

Trace: `lib/sha224-256.c:SHA256Reset`, `lib/sha224-256.c:SHA224_256Reset`

#### Scenario: 拒绝空 SHA-256 上下文
- **GIVEN** 调用方传入空 `context`
- **WHEN** 调用 `SHA256Reset(context)`
- **THEN** 函数返回 `shaNull` 且不写入上下文状态

Trace: `lib/sha224-256.c:SHA256Reset`, `lib/sha224-256.c:SHA224_256Reset`

### Requirement: SHA256Input append SHA-256 octets
系统 MUST 将非空输入字节追加到未完成且未损坏的 SHA-256 上下文，按 64 字节消息块压缩，并返回可观察的 SHA 错误码。

#### Scenario: 零长度输入直接成功
- **GIVEN** 任意 `context` 和 `message_array` 参数组合
- **WHEN** 调用 `SHA256Input(context, message_array, 0)`
- **THEN** 函数返回 `shaSuccess`，且不检查空指针、不修改上下文状态

Trace: `lib/sha224-256.c:SHA256Input`

#### Scenario: 拒绝非零长度空参数
- **GIVEN** `length` 大于 0，且 `context` 或 `message_array` 为空
- **WHEN** 调用 `SHA256Input(context, message_array, length)`
- **THEN** 函数返回 `shaNull`，且不执行消息块追加

Trace: `lib/sha224-256.c:SHA256Input`

#### Scenario: 拒绝 Result 或 FinalBits 后输入
- **GIVEN** `context->Computed` 已经为非零
- **WHEN** 调用 `SHA256Input(context, message_array, length)` 且 `length` 大于 0
- **THEN** 函数将 `context->Corrupted` 设置为 `shaStateError` 并返回 `shaStateError`

Trace: `lib/sha224-256.c:SHA256Input`

#### Scenario: 传播已损坏上下文状态
- **GIVEN** `context->Corrupted` 已经为非零且 `context->Computed` 为 0
- **WHEN** 调用 `SHA256Input(context, message_array, length)` 且 `length` 大于 0
- **THEN** 函数返回现有 `context->Corrupted` 值且不追加输入字节

Trace: `lib/sha224-256.c:SHA256Input`

### Requirement: SHA256FinalBits append SHA-256 trailing bits
系统 MUST 对 1 到 7 个尾部 bit 进行掩码、终止标记和最终化处理，并对空上下文、已计算状态或非法长度返回 SHA 错误码。

#### Scenario: 零长度 final bits 直接成功
- **GIVEN** 任意 `context` 参数
- **WHEN** 调用 `SHA256FinalBits(context, message_bits, 0)`
- **THEN** 函数返回 `shaSuccess`，且不检查空指针、不修改上下文状态

Trace: `lib/sha224-256.c:SHA256FinalBits`

#### Scenario: 拒绝空上下文 final bits
- **GIVEN** `context` 为空且 `length` 大于 0
- **WHEN** 调用 `SHA256FinalBits(context, message_bits, length)`
- **THEN** 函数返回 `shaNull` 且不执行最终化

Trace: `lib/sha224-256.c:SHA256FinalBits`

#### Scenario: 拒绝非法 final bits 状态或长度
- **GIVEN** 上下文已计算摘要，或 `length` 大于等于 8
- **WHEN** 调用 `SHA256FinalBits(context, message_bits, length)`
- **THEN** 函数将 `context->Corrupted` 设置为 `shaStateError` 并返回 `shaStateError`

Trace: `lib/sha224-256.c:SHA256FinalBits`

#### Scenario: 接受 1 到 7 个高位尾部 bit
- **GIVEN** 上下文未计算、未损坏，且 `length` 位于 1 到 7
- **WHEN** 调用 `SHA256FinalBits(context, message_bits, length)`
- **THEN** 函数仅保留 `message_bits` 的高位 `length` 个 bit，添加终止 bit，最终化上下文并返回 `shaSuccess`

Trace: `lib/sha224-256.c:SHA256FinalBits`, `lib/sha224-256.c:SHA224_256Finalize`

### Requirement: SHA256Result return 256-bit digest
系统 MUST 输出 32 字节 SHA-256 摘要，并在上下文尚未计算时先完成 padding、压缩和敏感缓冲区清理。

#### Scenario: 输出 SHA-256 摘要
- **GIVEN** 已通过 SHA-256 输入路径填充的上下文和非空 `Message_Digest` 缓冲区
- **WHEN** 调用 `SHA256Result(context, Message_Digest)`
- **THEN** 函数在需要时最终化上下文，按 big-endian 字节顺序写入 `SHA256HashSize` 个摘要字节，并返回 `shaSuccess`

Trace: `lib/sha224-256.c:SHA256Result`, `lib/sha224-256.c:SHA224_256ResultN`

#### Scenario: 拒绝空上下文或输出缓冲区
- **GIVEN** `context` 为空或 `Message_Digest` 为空
- **WHEN** 调用 `SHA256Result(context, Message_Digest)`
- **THEN** 函数返回 `shaNull` 且不写入摘要输出

Trace: `lib/sha224-256.c:SHA256Result`, `lib/sha224-256.c:SHA224_256ResultN`

#### Scenario: 传播已损坏上下文结果
- **GIVEN** `context->Corrupted` 已经为非零且 `Message_Digest` 非空
- **WHEN** 调用 `SHA256Result(context, Message_Digest)`
- **THEN** 函数返回现有 `context->Corrupted` 值且不最终化上下文

Trace: `lib/sha224-256.c:SHA256Result`, `lib/sha224-256.c:SHA224_256ResultN`

## Open Questions

| ID | Question | Related Interface | Reason |
| --- | --- | --- | --- |
| Q-001 | `SHA224Input`、`SHA224FinalBits` 和 `SHA224Result` 是否需要独立于 SHA-256 wrapper 语义记录额外测试向量？ | SHA224Input, SHA224FinalBits, SHA224Result | 当前 GitNexus 未定位到直接测试调用者，源码仅显示复用 SHA-256 实现。 |
| Q-002 | `SHA224_256AddLength` 在 2^64 bit 长度溢出时把 `Corrupted` 写为 `1` 而非 `shaInputTooLong` 是否为兼容契约？ | SHA256Input, SHA224Input, SHA256FinalBits, SHA224FinalBits | 源码宏可确认写入 `1`，但 header enum 将 `shaNull` 定义为 `1`，与注释中的 input-too-long 语义可能不一致。 |
| Q-003 | 零长度 `SHA256Input` 和 `SHA256FinalBits` 在空上下文时返回 `shaSuccess` 是否为有意公开行为？ | SHA256Input, SHA256FinalBits | 源码先检查 `!length` 再检查空指针，未发现测试或文档确认该边界。 |
