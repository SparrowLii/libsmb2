# lib/sha384-512.c Specification

## Source Context

- Source: `lib/sha384-512.c`
- Related Headers: `lib/sha.h`, `lib/sha-private.h`, `lib/compat.h`
- Related Tests: `none`
- Related Dependencies: `lib/usha.c` 调用 SHA384/SHA512 入口；`lib/hmac.c`、`lib/libsmb2.c`、`lib/smb2-signing.c` 通过统一 SHA/HMAC/SMB 认证和签名流程间接依赖 SHA-384/SHA-512 行为。GitNexus impact for `Function:lib/sha384-512.c:SHA512Input` reported CRITICAL risk, 15 upstream impacted symbols, 5 affected processes, and affected flows including session setup, negotiate, HMAC, and SMB2 signing.`
- Build/Compile Context: `lib/sha384-512.c` is compiled only when `USE_SHA384_SHA512` is defined and nonzero. `USE_32BIT_ONLY` selects 32-bit word emulation for SHA-384/SHA-512 arithmetic; otherwise native `uint64_t` words are used. `USE_MODIFIED_MACROS` may select algebraically equivalent SHA choice/majority macros. Project C standard is unknown from `PROJECT_CONTEXT.md`.

## Interface Summary

| Interface | Kind | Signature | Decision | Reason |
| --- | --- | --- | --- | --- |
| SHA384Reset | function | `extern int SHA384Reset (SHA384Context *);` | Include | 公开 SHA-384 生命周期入口，初始化上下文并决定后续摘要状态。 |
| SHA384Input | function | `extern int SHA384Input (SHA384Context *, const uint8_t * bytes, size_t bytecount);` | Include | 公开 SHA-384 数据输入入口，影响调用方可见的错误码、长度累计和摘要结果。 |
| SHA384FinalBits | function | `extern int SHA384FinalBits (SHA384Context *, const uint8_t bits, size_t bitcount);` | Include | 公开 SHA-384 非整字节尾部输入入口，具有边界和状态错误语义。 |
| SHA384Result | function | `extern int SHA384Result (SHA384Context *, uint8_t Message_Digest[SHA384HashSize]);` | Include | 公开 SHA-384 摘要输出入口，负责终结上下文并填充 48 字节摘要。 |
| SHA512Reset | function | `extern int SHA512Reset (SHA512Context *);` | Include | 公开 SHA-512 生命周期入口，初始化上下文并决定后续摘要状态。 |
| SHA512Input | function | `extern int SHA512Input (SHA512Context *, const uint8_t * bytes, size_t bytecount);` | Include | 公开 SHA-512 数据输入入口，被 SHA-384 转调并影响 SMB 认证、签名和 HMAC 流程。 |
| SHA512FinalBits | function | `extern int SHA512FinalBits (SHA512Context *, const uint8_t bits, size_t bitcount);` | Include | 公开 SHA-512 非整字节尾部输入入口，具有边界和状态错误语义。 |
| SHA512Result | function | `extern int SHA512Result (SHA512Context *, uint8_t Message_Digest[SHA512HashSize]);` | Include | 公开 SHA-512 摘要输出入口，负责终结上下文并填充 64 字节摘要。 |
| SHA384_512Finalize | function | `static void SHA384_512Finalize (SHA512Context * context, uint8_t Pad_Byte);` | Skip | 静态内部 helper，行为归属到 Result/FinalBits 公开接口。 |
| SHA384_512PadMessage | function | `static void SHA384_512PadMessage (SHA512Context * context, uint8_t Pad_Byte);` | Skip | 静态内部 helper，填充规则对外通过摘要输出体现。 |
| SHA384_512ProcessMessageBlock | function | `static void SHA384_512ProcessMessageBlock (SHA512Context * context);` | Skip | 静态压缩函数，属于内部实现细节。 |
| SHA384_512Reset | function | `static int SHA384_512Reset (SHA512Context * context, uint64_t H0[]);` | Skip | 静态内部 helper，错误码和初始化语义由 Reset 公开接口承诺。 |
| SHA384_512ResultN | function | `static int SHA384_512ResultN (SHA512Context * context, uint8_t Message_Digest[], int HashSize);` | Skip | 静态内部 helper，输出长度由 SHA384Result/SHA512Result 公开接口承诺。 |

## Data Model Summary

| Type/Macro | Kind | Definition | Notes |
| --- | --- | --- | --- |
| SHA384_H0 | static data | `lib/sha384-512.c:244` | SHA-384 初始哈希值；32-bit 或 64-bit 表示随 `USE_32BIT_ONLY` 分支变化，但对调用方摘要语义应等价。 |
| SHA512_H0 | static data | `lib/sha384-512.c:251` | SHA-512 初始哈希值；32-bit 或 64-bit 表示随 `USE_32BIT_ONLY` 分支变化，但对调用方摘要语义应等价。 |
| SHA384_512AddLength | macro | `lib/sha384-512.c:229` | 内部长度累计宏；溢出时设置 `Corrupted`，公开表现为后续错误码。 |
| SHA512_SHR | macro | `lib/sha384-512.c:59` | 内部 SHA-512 位移宏；32-bit 和 64-bit 分支均用于压缩轮计算。 |
| SHA512_ROTR | macro | `lib/sha384-512.c:137` | 内部 SHA-512 循环右移宏；32-bit 和 64-bit 分支均用于压缩轮计算。 |
| SHA512_SIGMA0 | macro | `lib/sha384-512.c:146` | 内部 FIPS 180-2 SHA-512 大写 Sigma0 计算。 |
| SHA512_SIGMA1 | macro | `lib/sha384-512.c:156` | 内部 FIPS 180-2 SHA-512 大写 Sigma1 计算。 |
| SHA512_sigma0 | macro | `lib/sha384-512.c:166` | 内部 FIPS 180-2 SHA-512 小写 sigma0 计算。 |
| SHA512_sigma1 | macro | `lib/sha384-512.c:176` | 内部 FIPS 180-2 SHA-512 小写 sigma1 计算。 |

## ADDED Requirements

### Requirement: SHA384Reset initialize SHA-384 context
系统 MUST 在收到非空 `SHA384Context` 时使用 SHA-384 初始哈希值初始化上下文，清零消息块索引和长度状态，并将 `Computed` 与 `Corrupted` 置为未完成且未损坏状态；系统 MUST 在 `context` 为空时返回 `shaNull`。

#### Scenario: reset valid SHA-384 context
- **GIVEN** 调用方提供一个可写的 `SHA384Context`。
- **WHEN** 调用方调用 `SHA384Reset(context)`。
- **THEN** 函数返回 `shaSuccess`，上下文准备好接收新的 SHA-384 输入。

Trace: `lib/sha384-512.c:SHA384Reset`, `lib/sha384-512.c:SHA384_512Reset`, `lib/sha.h:SHA384Context`

#### Scenario: reject null SHA-384 context
- **GIVEN** 调用方没有提供有效上下文。
- **WHEN** 调用方调用 `SHA384Reset(NULL)`。
- **THEN** 函数返回 `shaNull`，且不解引用空指针。

Trace: `lib/sha384-512.c:SHA384Reset`, `lib/sha384-512.c:SHA384_512Reset`

### Requirement: SHA384Input accept SHA-384 octet input
系统 MUST 将 `SHA384Input` 作为 SHA-384 字节输入入口，并 MUST 将其错误码、空输入行为、状态错误行为和长度累计行为与同一上下文上的 `SHA512Input` 保持一致。

#### Scenario: input zero SHA-384 bytes
- **GIVEN** 调用方传入任意上下文指针和 `length == 0`。
- **WHEN** 调用方调用 `SHA384Input(context, bytes, 0)`。
- **THEN** 函数返回 `shaSuccess`，且不要求 `context` 或 `bytes` 非空。

Trace: `lib/sha384-512.c:SHA384Input`, `lib/sha384-512.c:SHA512Input`

#### Scenario: input nonzero SHA-384 bytes
- **GIVEN** 调用方已通过 `SHA384Reset` 初始化上下文并提供非空消息缓冲区。
- **WHEN** 调用方调用 `SHA384Input(context, bytes, bytecount)` 且 `bytecount > 0`。
- **THEN** 函数按字节追加消息、按 8 bit 累计长度，并在 1024-bit 消息块填满时处理该块。

Trace: `lib/sha384-512.c:SHA384Input`, `lib/sha384-512.c:SHA512Input`, `lib/sha384-512.c:SHA384_512ProcessMessageBlock`

#### Scenario: reject invalid SHA-384 input state
- **GIVEN** 调用方提供空上下文、空消息缓冲区、已计算完成的上下文或已损坏的上下文之一。
- **WHEN** 调用方调用 `SHA384Input(context, bytes, bytecount)` 且 `bytecount > 0`。
- **THEN** 函数返回 `shaNull`、`shaStateError` 或上下文已有的 `Corrupted` 错误码；若上下文已完成，函数将 `Corrupted` 设置为 `shaStateError`。

Trace: `lib/sha384-512.c:SHA384Input`, `lib/sha384-512.c:SHA512Input`

### Requirement: SHA384FinalBits finalize SHA-384 partial-byte input
系统 MUST 将 `SHA384FinalBits` 作为 SHA-384 1 到 7 bit 尾部输入入口，并 MUST 将其错误码、空 bitcount 行为、状态错误行为和最终填充行为与同一上下文上的 `SHA512FinalBits` 保持一致。

#### Scenario: finalize SHA-384 with no final bits
- **GIVEN** 调用方传入任意上下文指针并指定 `bitcount == 0`。
- **WHEN** 调用方调用 `SHA384FinalBits(context, bits, 0)`。
- **THEN** 函数返回 `shaSuccess`，且不要求 `context` 非空。

Trace: `lib/sha384-512.c:SHA384FinalBits`, `lib/sha384-512.c:SHA512FinalBits`

#### Scenario: finalize SHA-384 with one to seven bits
- **GIVEN** 调用方提供未完成且未损坏的 SHA-384 上下文，并将最终 bit 放在 `bits` 的高位部分。
- **WHEN** 调用方调用 `SHA384FinalBits(context, bits, bitcount)` 且 `1 <= bitcount <= 7`。
- **THEN** 函数累计最终 bit 长度，使用被掩码保留的高位 bit 加终止标记构造填充字节，完成摘要终结，并返回 `shaSuccess`。

Trace: `lib/sha384-512.c:SHA384FinalBits`, `lib/sha384-512.c:SHA512FinalBits`, `lib/sha384-512.c:SHA384_512Finalize`

#### Scenario: reject invalid SHA-384 final bits
- **GIVEN** 调用方提供空上下文、已完成上下文、已损坏上下文或 `bitcount >= 8` 之一。
- **WHEN** 调用方调用 `SHA384FinalBits(context, bits, bitcount)`。
- **THEN** 函数返回 `shaNull`、`shaStateError` 或上下文已有的 `Corrupted` 错误码；已完成上下文或过大 bitcount 会将 `Corrupted` 设置为 `shaStateError`。

Trace: `lib/sha384-512.c:SHA384FinalBits`, `lib/sha384-512.c:SHA512FinalBits`

### Requirement: SHA384Result produce 384-bit digest
系统 MUST 在 `SHA384Result` 收到有效上下文和输出缓冲区时返回 48 字节 SHA-384 摘要，并 MUST 在上下文尚未完成时先执行 SHA-384/SHA-512 填充终结；系统 MUST 对空上下文或空输出缓冲区返回 `shaNull`。

#### Scenario: produce SHA-384 digest
- **GIVEN** 调用方已初始化 SHA-384 上下文并输入零个或多个字节和可选最终 bit。
- **WHEN** 调用方调用 `SHA384Result(context, Message_Digest)` 且输出缓冲区可写至少 `SHA384HashSize` 字节。
- **THEN** 函数返回 `shaSuccess`，将摘要按高位字节优先顺序写入 `Message_Digest[0..47]`，并在需要时终结上下文。

Trace: `lib/sha384-512.c:SHA384Result`, `lib/sha384-512.c:SHA384_512ResultN`, `lib/sha384-512.c:SHA384_512Finalize`

#### Scenario: reject invalid SHA-384 result request
- **GIVEN** 调用方提供空上下文、空输出缓冲区或已损坏上下文之一。
- **WHEN** 调用方调用 `SHA384Result(context, Message_Digest)`。
- **THEN** 函数返回 `shaNull` 或上下文已有的 `Corrupted` 错误码，且不生成新的成功摘要。

Trace: `lib/sha384-512.c:SHA384Result`, `lib/sha384-512.c:SHA384_512ResultN`

### Requirement: SHA512Reset initialize SHA-512 context
系统 MUST 在收到非空 `SHA512Context` 时使用 SHA-512 初始哈希值初始化上下文，清零消息块索引和长度状态，并将 `Computed` 与 `Corrupted` 置为未完成且未损坏状态；系统 MUST 在 `context` 为空时返回 `shaNull`。

#### Scenario: reset valid SHA-512 context
- **GIVEN** 调用方提供一个可写的 `SHA512Context`。
- **WHEN** 调用方调用 `SHA512Reset(context)`。
- **THEN** 函数返回 `shaSuccess`，上下文准备好接收新的 SHA-512 输入。

Trace: `lib/sha384-512.c:SHA512Reset`, `lib/sha384-512.c:SHA384_512Reset`, `lib/sha.h:SHA512Context`

#### Scenario: reject null SHA-512 context
- **GIVEN** 调用方没有提供有效上下文。
- **WHEN** 调用方调用 `SHA512Reset(NULL)`。
- **THEN** 函数返回 `shaNull`，且不解引用空指针。

Trace: `lib/sha384-512.c:SHA512Reset`, `lib/sha384-512.c:SHA384_512Reset`

### Requirement: SHA512Input accept SHA-512 octet input
系统 MUST 将 `SHA512Input` 作为 SHA-512 字节输入入口，按字节追加消息、按 8 bit 累计长度，并在每个 1024-bit 消息块填满时执行 SHA-512 压缩处理。

#### Scenario: input zero SHA-512 bytes
- **GIVEN** 调用方传入任意上下文指针和 `length == 0`。
- **WHEN** 调用方调用 `SHA512Input(context, bytes, 0)`。
- **THEN** 函数返回 `shaSuccess`，且不要求 `context` 或 `bytes` 非空。

Trace: `lib/sha384-512.c:SHA512Input`

#### Scenario: input nonzero SHA-512 bytes
- **GIVEN** 调用方已通过 `SHA512Reset` 初始化上下文并提供非空消息缓冲区。
- **WHEN** 调用方调用 `SHA512Input(context, bytes, bytecount)` 且 `bytecount > 0`。
- **THEN** 函数逐字节写入 `Message_Block`，累计 bit 长度，并在块索引达到 `SHA512_Message_Block_Size` 时调用压缩处理。

Trace: `lib/sha384-512.c:SHA512Input`, `lib/sha384-512.c:SHA384_512ProcessMessageBlock`

#### Scenario: reject invalid SHA-512 input state
- **GIVEN** 调用方提供空上下文、空消息缓冲区、已计算完成的上下文或已损坏的上下文之一。
- **WHEN** 调用方调用 `SHA512Input(context, bytes, bytecount)` 且 `bytecount > 0`。
- **THEN** 函数返回 `shaNull`、`shaStateError` 或上下文已有的 `Corrupted` 错误码；若上下文已完成，函数将 `Corrupted` 设置为 `shaStateError`。

Trace: `lib/sha384-512.c:SHA512Input`

### Requirement: SHA512FinalBits finalize SHA-512 partial-byte input
系统 MUST 支持通过 `SHA512FinalBits` 添加 1 到 7 个最终 bit，且这些 bit MUST 来自 `message_bits` 的高位部分；系统 MUST 对空上下文、已完成上下文或 `length >= 8` 返回错误。

#### Scenario: finalize SHA-512 with no final bits
- **GIVEN** 调用方传入任意上下文指针并指定 `length == 0`。
- **WHEN** 调用方调用 `SHA512FinalBits(context, message_bits, 0)`。
- **THEN** 函数返回 `shaSuccess`，且不要求 `context` 非空。

Trace: `lib/sha384-512.c:SHA512FinalBits`

#### Scenario: finalize SHA-512 with one to seven bits
- **GIVEN** 调用方提供未完成且未损坏的 SHA-512 上下文，并将最终 bit 放在 `message_bits` 的高位部分。
- **WHEN** 调用方调用 `SHA512FinalBits(context, message_bits, length)` 且 `1 <= length <= 7`。
- **THEN** 函数累计最终 bit 长度，使用被掩码保留的高位 bit 加终止标记构造填充字节，完成摘要终结，并返回 `shaSuccess`。

Trace: `lib/sha384-512.c:SHA512FinalBits`, `lib/sha384-512.c:SHA384_512Finalize`

#### Scenario: reject invalid SHA-512 final bits
- **GIVEN** 调用方提供空上下文、已完成上下文、已损坏上下文或 `length >= 8` 之一。
- **WHEN** 调用方调用 `SHA512FinalBits(context, message_bits, length)`。
- **THEN** 函数返回 `shaNull`、`shaStateError` 或上下文已有的 `Corrupted` 错误码；已完成上下文或过大 length 会将 `Corrupted` 设置为 `shaStateError`。

Trace: `lib/sha384-512.c:SHA512FinalBits`

### Requirement: SHA512Result produce 512-bit digest
系统 MUST 在 `SHA512Result` 收到有效上下文和输出缓冲区时返回 64 字节 SHA-512 摘要，并 MUST 在上下文尚未完成时先执行 SHA-512 填充终结；系统 MUST 对空上下文或空输出缓冲区返回 `shaNull`。

#### Scenario: produce SHA-512 digest
- **GIVEN** 调用方已初始化 SHA-512 上下文并输入零个或多个字节和可选最终 bit。
- **WHEN** 调用方调用 `SHA512Result(context, Message_Digest)` 且输出缓冲区可写至少 `SHA512HashSize` 字节。
- **THEN** 函数返回 `shaSuccess`，将摘要按高位字节优先顺序写入 `Message_Digest[0..63]`，并在需要时终结上下文。

Trace: `lib/sha384-512.c:SHA512Result`, `lib/sha384-512.c:SHA384_512ResultN`, `lib/sha384-512.c:SHA384_512Finalize`

#### Scenario: reject invalid SHA-512 result request
- **GIVEN** 调用方提供空上下文、空输出缓冲区或已损坏上下文之一。
- **WHEN** 调用方调用 `SHA512Result(context, Message_Digest)`。
- **THEN** 函数返回 `shaNull` 或上下文已有的 `Corrupted` 错误码，且不生成新的成功摘要。

Trace: `lib/sha384-512.c:SHA512Result`, `lib/sha384-512.c:SHA384_512ResultN`

## Open Questions

| ID | Question | Related Interface | Reason |
| --- | --- | --- | --- |
| Q-001 | 是否存在覆盖 SHA-384/SHA-512 RFC 4634 测试向量的项目内测试文件？ | SHA384Result, SHA512Result | GitNexus context 未返回测试调用者，当前回读只确认源码和上游生产调用。 |
| Q-002 | `USE_32BIT_ONLY` 和原生 `uint64_t` 分支是否在所有目标平台上要求逐字节相同摘要输出？ | file-level | 源码注释说明两种实现路径，未在当前文件内提供跨分支测试证据。 |
